# Local dev env for payphone-ollama-tts
# Copy to .env and fill in.

# Hosted Ollama
OLLAMA_BASE_URL=http://localhost:11434

# Inworld TTS
# Inworld uses Basic auth with a base64 credential string.
# Set this to the base64 credential (NOT "Basic ...")
INWORLD_API_KEY=
INWORLD_VOICE_ID=Timothy
INWORLD_MODEL_ID=inworld-tts-1.5-mini

# Deepgram STT (optional for mic input)
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-2
DEEPGRAM_LANGUAGE=en

# KittenTTS (local, no API key)
# Option A (recommended): run a separate persistent microservice and point payphone at it.
KITTENTTS_BASE_URL=http://127.0.0.1:9123
KITTENTTS_VOICE=Jasper

# Option B (fallback): per-request python spawn (slower; no service)
# Requires Python 3.12 and installing KittenTTS (see README)
KITTENTTS_PYTHON=python3
KITTENTTS_MODEL=KittenML/kitten-tts-mini-0.8

# Server
PORT=5174
